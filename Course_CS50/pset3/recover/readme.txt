Спочатку ми хочемо відкрити файл на карі пам'яті.
Потім повторювати, поки ми не дійдемо до кінця файлу.
Ми читаємо по 512 байтів.
Потім запитуємо, чи ми на початку нового JPEG - файлу.
Якщо так, ми питаємо, чи ми вже знайшли JPEG-файл.
Якщо ні, то це означає, що ми розпочинаємо найперший JPEG.
Але якщо ми вже маємо JPEG, тоді
це означає, що ми маємо закрити попередній файл та почати новий.
Уявімо, що ми прочитали 512 байтів та не знайшли
початок нового JPEG-файлу.
Тоді ми запитаємо, чи ми знайшли JPEG, чи не знайшли?
Тому що якщо ні, то це означає, що ми можемо просто
не брати до уваги ці 512 байтів та повернутись на початок циклу.
Але якщо ми вже знайшли JPEG, тоді
це означає, що ці 512 ьбйтів належать до відкритого файлу.
нарешті, коли ми дістанемось кінця карти пам'яті, ми можемо вийти з циклу
та закрити всі файли, що залишились.
